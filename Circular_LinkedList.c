#include<stsio.h>
#include<stdlib.h>
struct Node{
    int data;
    struct Node *next;
}
void createLL(struct Node **head , int data){
    struct Node *newNode = malloc(sizeof(struct Node));
    
}
int main(){
    return 0;
}